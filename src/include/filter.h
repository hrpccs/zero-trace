#ifndef FILTER_H
#define FILTER_H

#ifndef DEVICE_FILTER
#define DEVICE_FILTER(x) ((x) != (x))
#endif

#ifndef TID_FILTER
#define TID_FILTER(x) ((x) != (x))
#endif

#ifndef TGID_FILTER
#define TGID_FILTER(x) ((x) != (x))
#endif

#endif
